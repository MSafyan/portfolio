JOB DETAILS:

We’ve built the foundation of an AI chat application using Lovable. The app already allows users to:

Sign up / sign in with Supabase Auth (Google/Apple)

Create a AI persona

Now we need to edit and tweak the Retrieval-Augmented Generation (RAG), Pinecone vector database integration, and add AI safety guardrails.

Scope of Work

Edit RAG Integration and Pinecone

Build ingestion pipeline: split text, embed with OpenAI, upsert with metadata (persona_id, visibility, type, timestamps).

Retrieval pipeline: query per persona with filters (persona_id, visibility, safety tags), re-rank, and pass context into the LLM.

Guardrails & Privacy

Add ingestion redaction: scrub emails, phone numbers, addresses before embedding.

Retrieval filters: prevent cross-persona leakage, exclude vectors tagged as PII/private.

Policy middleware: block unsafe requests (PII requests, disallowed content) before calling the LLM.

Post-processing: scan LLM output for unsafe disclosures.

LLM UX Tuning

Implement hybrid conversational style: responses should both answer and ask clarifying questions to capture more user context over time.

Configure prompt templates and token budgets for responsiveness and cost control.

Testing & Deployment

Write clear logging (retrieved vector IDs, reasons for blocked requests).

Ensure latency for RAG-enhanced chat is 2.5s.

Help set up environment variables correctly for Supabase Edge Functions and Lovable.

Requirements

Experience with RAG architectures (OpenAI + Pinecone or pgvector).

Proficiency in Node.js/TypeScript (Supabase Edge Functions, Next.js API routes).

Familiarity with Supabase auth + storage.

Strong understanding of LLM moderation/guardrails.

Ability to document implementation and provide clear hand-off notes.

Deliverables

Working RAG pipeline (ingestion + retrieval) integrated into the current app.

Guardrail system (pre-check, redaction, retrieval filter, post-check).

Updated ingestion and chat endpoints.

Documentation for how to safely add new personas.

Acceptance test checklist for privacy + safety.


-------------------------------------------------------------------------------------------------
PROPOSAL:


Hey there,
Have been working on a similar project where we are trying to match users based on there interests and profile data stored in PInecone. Our flow is UserData -> GPT-3.5 for summary/strengths/keyWords -> text Embedding using OpenAI embedding-text-3 -> vectors stored in PineCone. Then other users asks for similar user and we make an api call to pinecone and get similar Users.

Having been working on a similar project I think we can reuse some techniques found there and avoid some problems like trying to store all data in pineCone instead summarized version using LLMs. and bootstrap this faster.

I have 5+ years of experience in Nextjs/React/Typescript and have AWS Solution Architect Certification.

Drop me a message if you wanna know what problems we faced and how we are trying to solve those problems.