Seeking an expert data scraper to build a comprehensive contact database of U.S. franchised automotive dealerships. The task involves direct scraping and research using LLMs and automation tools to gather verified contact information from dealership websites, LinkedIn, and public sources.

Deliverables
Identify all franchised dealerships in the U.S.
Capture key roles and contact information
Organize data into structured columns
Use LLMs and automation tools for data extraction

---

Proposal:

Hey there,

First, I’ll compile a master list of franchised stores from OEM dealer locators, large dealer groups, state dealer lists, and MarketCheck. That gives us clean names, domains, addresses, and the brands each store represents. In other project, we show all recent car listings from franchised dealerships to get good deals using MarketCheck API. 

With that foundation, I’ll crawl each website carefully using Playwright—rotating proxies, realistic headers, and backoff so we’re resilient and respectful. Instead of scraping free‑form text, I’ll extract into a strict schema (role, name, title, email, phone, department, source URL) and let an LLM help normalize job titles and departments. Where LLMs are uncertain, regex and heuristics catch the basics, and anything low‑confidence is flagged for a quick review.

Every contact gets verified and deduplicated. I’ll run email verification, match domains to websites, and score each record so you can see not just the result but also how trustworthy it is. Deliverables come as clean CSV/Parquet or a Postgres dump with the source URL and timestamp for every field, plus a simple status view so you know what’s covered and what’s left.

This runs on a pragmatic stack: Python with Playwright for the crawling and OpenAI for structured extraction and normalization. Once the first pass is complete, we schedule incremental refreshes so the data stays current without a full re‑crawl.

I can start with a quick proof of concept over a couple of brands this week so you can judge coverage and accuracy before we scale nationwide.

Best regards,