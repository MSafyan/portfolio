Hey there,

I can work for free for the first few days so you can validate the fit and momentum before we start a contract. You’re building a password‑manager‑style SaaS on Next.js/Node.js with Prisma/SQL and secure auth (Clerk/Auth0) on AWS or Azure—that’s right in my wheelhouse as a fullStack dev with real fullStack experience shipping secure SaaS apps end‑to‑end.

Quick background: I’ve built and maintained production SaaS with Next.js/TypeScript, designed Prisma/TypeORM schemas (migrations, indexing, query tuning), and implemented auth with Firebase/Web3/Cognito (sessions/JWT, SSO, roles/permissions). On the cloud side, I’ve deployed to AWS (EC2, RDS Postgres, S3, CloudWatch) and Azure SQL with clean CI/CD, staging/prod environments, and monitoring.

If we work together, here’s how I’d approach it:
- Start with a clean Next.js foundation and a pragmatic Prisma schema (users, orgs, vaults, items, audit events)
- Wire up the chosen auth provider (Clerk/Auth0) with a simple, flexible role/permission model
- Build the core flows: create/share vaults, add/search items, soft‑delete with audit logging
- Bake in security from day one: hashing, per‑field encryption, managed keys (KMS/Key Vault), secret rotation
- Wrap it with CI/CD, staging/prod, observability, input validation and rate limiting

Two quick questions:
1) Do you prefer Clerk or Auth0 for the MVP, and do you need SSO (Google/Microsoft) initially?
2) Should we go multi‑tenant from day one, or ship single‑tenant first with a straightforward path to orgs/sharing?

I can start today and share daily updates. During the free trial, I’m happy to spin up the initial Prisma schema and auth integration so you can see real progress fast.
