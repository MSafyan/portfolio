// Ball.jsx is no longer used — Tech icons are rendered as flat cards in Tech.jsx.
// Kept as an empty re-export to avoid breaking any stray imports.

const BallCanvas = () => null;
export default BallCanvas;
